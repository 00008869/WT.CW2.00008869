mixin link(href, name)
  a(href=href)&attributes(attributes)= name

header.navbar
  h1.logo 
    a(href="/") HR application

  nav.nav
    ul.nav-items
      li.nav-item 
        +link('/notes', 'All applicants')(class="nav-link")
      li.nav-item 
        +link('/archive', 'Archive')(class="nav-link")